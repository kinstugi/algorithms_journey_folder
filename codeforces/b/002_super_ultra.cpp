#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    cin.tie(0)->sync_with_stdio(0);
    freopen("sample.in", "r", stdin);

    int t, n;
    cin >> t;
    while (t--){
        cin >> n;
        vector<int> evens;
    }
    return 0;
}